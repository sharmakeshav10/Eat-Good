import { ShimmerPostList } from "react-shimmer-effects";

const RestaurantCardShimmer = () => {
  return <ShimmerPostList postStyle="STYLE_THREE" col={4} row={2} gap={30} />;
};

export default RestaurantCardShimmer;
